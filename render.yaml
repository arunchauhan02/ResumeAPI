services:
  - type: web
    name: resume-parser-flask
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: GOOGLE_API_KEY
        value: your_actual_api_key_here
